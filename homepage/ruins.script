let depth = 0;
const stage = document.getElementById("stage");

window.addEventListener("wheel", e => {
  depth += e.deltaY * 0.6;
  depth = Math.min(Math.max(depth, -1400), 200);
  stage.style.transform = `translateZ(${depth}px)`;
}, { passive:false });
