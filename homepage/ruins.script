let depth = 0;
const stage = document.getElementById("stage");

window.addEventListener("wheel", e=>{
  depth += e.deltaY * -0.3;
  depth = Math.max(-500, Math.min(300, depth));
  stage.style.transform = `translateZ(${depth}px)`;
});
