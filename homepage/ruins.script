let depth = 0;
const stage = document.getElementById("stage");

window.addEventListener("wheel", e=>{
  depth += e.deltaY * -0.5;
  depth = Math.max(-800, Math.min(400, depth));
  stage.style.transform = `translateZ(${depth}px)`;
});
