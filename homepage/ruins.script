let depth = 0;
const world = document.getElementById("world");

window.addEventListener("wheel", e => {
  depth += e.deltaY * -0.5;
  depth = Math.max(Math.min(depth, 300), -1000);
  world.style.setProperty("--z", depth + "px");
});
