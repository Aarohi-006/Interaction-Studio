let depth = 0;
const scene = document.getElementById("scene");

window.addEventListener("wheel", e => {
  depth += e.deltaY * -0.8;
  depth = Math.max(Math.min(depth, 300), -1600);
  scene.style.setProperty("--depth", depth + "px");
}, { passive:true });
